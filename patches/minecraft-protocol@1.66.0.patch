diff --git a/src/datatypes/lpVec3.js b/src/datatypes/lpVec3.js
index 9e0eb1408a3213e3ea263f66e52b69e4936beeca..df266ea4cd98b7a5df2b523a603131cd760b871f 100644
--- a/src/datatypes/lpVec3.js
+++ b/src/datatypes/lpVec3.js
@@ -28,7 +28,7 @@ function readLpVec3 (buffer, offset) {
   }
 
   const b = buffer[offset + 1]
-  const c = buffer.readUInt32LE(offset + 2)
+  const c = buffer.readUInt32BE(offset + 2)
 
   // Combine into 48-bit safe integer (up to 2^53 is safe in JS)
   const packed = (c * 65536) + (b << 8) + a
@@ -75,14 +75,15 @@ function writeLpVec3 (value, buffer, offset) {
   // Layout:
   // [Z (15)] [Y (15)] [X (15)] [Flags (3)]
 
-  // low32 contains Flags(3), X(15), and the first 14 bits of Y (3+15+14 = 32)
-  const low32 = (scaleByte | (pX << 3) | (pY << 18)) >>> 0
+  // low16 contains Flags(3) + lower 13 bits of X
+  const low16 = (scaleByte | (pX << 3)) & 0xFFFF
 
-  // high16 contains the 15th bit of Y and all 15 bits of Z
-  const high16 = ((pY >> 14) & 0x01) | (pZ << 1)
+  // high32 contains upper 2 bits of X, all 15 bits of Y, and all 15 bits of Z
+  const high32 = ((pX >> 13) | (pY << 2) | (pZ << 17)) >>> 0
 
-  buffer.writeUInt32LE(low32, offset)
-  buffer.writeUInt16LE(high16, offset + 4)
+  buffer[offset] = low16 & 0xFF
+  buffer[offset + 1] = (low16 >> 8) & 0xFF
+  buffer.writeUInt32BE(high32, offset + 2)
 
   if (needsContinuation) {
     return writeVarInt(Math.floor(scale / 4), buffer, offset + 6)
